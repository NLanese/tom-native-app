import { StyleSheet } from 'react-native';

const settings = StyleSheet.create({
    container: {
        width: '100%',
        backgroundColor: '#1a2c3d'
    },
    text: {
        color: 'white'
    }
})

export {
    settings
}